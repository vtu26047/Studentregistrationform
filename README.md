
  # Student Registration Form

  This is a code bundle for Student Registration Form. The original project is available at https://www.figma.com/design/kPBACGOlgi9mPTQKK4tkus/Student-Registration-Form.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  